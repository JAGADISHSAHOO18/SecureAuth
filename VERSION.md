# SecureAuth v17

- OAuth2 profile-gated configuration (`prod,oauth`)
- Docker Compose no longer hard-fails on `docker compose down` when OAuth secrets are absent
- Google and GitHub OAuth callbacks remain wired for localhost:8082
- SecureAuth branding retained
