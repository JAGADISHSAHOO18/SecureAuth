spring:
  datasource:
    url: ${DB_URL:jdbc:mysql://localhost:3306/auth_app?createDatabaseIfNotExist=true&serverTimezone=UTC}
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:root}
  jpa.hibernate.ddl-auto: update
  jpa.open-in-view: false
